language: C

compiler:
  - clang
  - gcc

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libxml2-dev libglib2.0-dev intltool gtk-doc-tools

script: ./autogen.sh && ./configure && make && make test
